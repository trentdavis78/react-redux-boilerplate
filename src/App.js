import React from 'react';
import { hot } from 'react-hot-loader';

const App = () => {

    return (
        <div>
            <h1>React Redux</h1>
        </div>
    )
}

export default hot(module)(App);